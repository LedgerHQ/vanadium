[package]
name = "vanadium-riscv-arch-tests"
version = "0.1.0"
edition = "2021"
description = "Official RISC-V architectural tests for the Vanadium CPU emulator"

[dependencies]
common = { path = "../common", features = ["target_native"] }
goblin = "0.8"

[dev-dependencies]
# no extra dev-deps; test functions are generated by build.rs and included into tests/
